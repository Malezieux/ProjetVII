{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
{% include 'navbar.php'%}
    <h1 className='red-500'>Mon profil</h1>
    <table class="table">
        <tbody>
{% if is_granted('ROLE_ADMIN') %}
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>

                <th>Nom </th>
                <td>{{ user.nom }}</td>
            </tr>

             
            {% endif %}
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Formation</th>
                <td>{{ user.formation }}</td>
            </tr>
            <tr>
                <th>Promotion</th>
                <td>{{ user.promotion }}</td>
            </tr>
        </tbody>
    </table>

 <a href="{{ path('app_profil_edit', {'id': user.id}) }}">edit</a>

    <a href="{{ path('app_profil_index') }}">back to list</a>


    {{ include('profil/_delete_form.html.twig') }}
{% endblock %}
